package lt.ffda.revancedcligui.tasks;

public interface ResourceCheckCallback {

    /**
     * Callback method to be used with resource check for a call back to caller class
     */
    void callback();
}
